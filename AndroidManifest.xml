<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="net.bensdeals"
          android:versionCode="5"
          android:versionName="1.0.3">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="com.google.android.gm.permission.AUTO_SEND"/>
    <uses-sdk android:minSdkVersion="1" android:targetSdkVersion="8"/>

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:name=".activity.DealApplication"
        android:theme="@android:style/Theme.Black.NoTitleBar">

        <activity
            android:name=".activity.DealPagerActivity"
            android:label="@string/app_name"
            android:configChanges="keyboardHidden|orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.DealSearchActivity"
            android:configChanges="keyboardHidden|orientation"
            android:windowSoftInputMode="adjustPan"/>
    </application>
</manifest>
